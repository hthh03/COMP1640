@model WebApplication2.Models.Users
@{
    ViewData["Title"] = "Create New User";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0 rounded-lg">
                <div class="card-header bg-gradient-primary-to-secondary">
                    <h2 class="text-center text-white font-weight-light my-2">Create New User</h2>
                </div>
                <div class="card-body p-4">
                    <form asp-action="CreateUser" asp-controller="Admin" method="post">
                        <div class="mb-4">
                            <label class="form-label small text-muted">Full Name</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-user"></i></span>
                                <input type="text" name="FullName" class="form-control" required placeholder="Enter full name" />
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small text-muted">Email Address</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                                <input type="email" name="Email" class="form-control" required placeholder="Enter email address" />
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small text-muted">Password</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                <input type="password" name="Password" class="form-control" required placeholder="Enter password" />
                                <button class="btn btn-outline-secondary" type="button" id="togglePassword">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                            <div class="form-text">Password should be at least 8 characters long</div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label small text-muted">User Role</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="fas fa-user-tag"></i></span>
                                <select name="Role" class="form-select">
                                    <option value="Student">Student</option>
                                    <option value="Teacher">Teacher</option>
                                </select>
                            </div>
                        </div>

                        <div class="d-flex align-items-center justify-content-between mt-4 mb-0">
                            <a class="small text-decoration-none" href="javascript:history.back()">Cancel</a>
                            <button type="submit" class="btn btn-primary px-4">
                                <i class="fas fa-user-plus me-2"></i>Create User
                            </button>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-center py-3 bg-light">
                    <div class="small">All fields marked with * are required</div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.getElementById('togglePassword').addEventListener('click', function () {
        const passwordInput = document.querySelector('input[name="Password"]');
        const icon = this.querySelector('i');

        if (passwordInput.type === 'password') {
            passwordInput.type = 'text';
            icon.classList.remove('fa-eye');
            icon.classList.add('fa-eye-slash');
        } else {
            passwordInput.type = 'password';
            icon.classList.remove('fa-eye-slash');
            icon.classList.add('fa-eye');
        }
    });
</script>

<!-- Add this to your layout or view to include Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<!-- Link to your custom CSS file -->
<link rel="stylesheet" href="~/css/admin.css">

<!-- Responsive CSS -->
<style>
    /* Global responsive adjustments */
    @@media (max-width: 1199.98px) {
        .container

    {
        padding-left: 20px;
        padding-right: 20px;
    }

    }

    /* Tablet styles (768px - 991.98px) */
    @@media (max-width: 991.98px) {
        .col-lg-6

    {
        max-width: 80%;
        flex: 0 0 80%;
    }

    .card-body {
        padding: 1.5rem !important;
    }

    h2 {
        font-size: 1.5rem;
    }

    }

    /* Small tablet styles (576px - 767.98px) */
    @@media (max-width: 767.98px) {
        .col-md-8

    {
        max-width: 90%;
        flex: 0 0 90%;
    }

    .py-5 {
        padding-top: 2rem !important;
        padding-bottom: 2rem !important;
    }

    .card-body {
        padding: 1.25rem !important;
    }

    }

    /* Mobile styles (< 576px) */
    @@media (max-width: 575.98px) {
        .container

    {
        padding-left: 15px;
        padding-right: 15px;
    }

    .col-md-8 {
        max-width: 100%;
        flex: 0 0 100%;
    }

    .card {
        border-radius: 0.5rem !important;
    }

    .card-body {
        padding: 1rem !important;
    }

    h2 {
        font-size: 1.3rem;
    }

    .input-group {
        flex-wrap: nowrap;
    }

    .d-flex.align-items-center.justify-content-between {
        flex-direction: column;
        gap: 1rem;
    }

        .d-flex.align-items-center.justify-content-between .btn {
            width: 100%;
        }

        .d-flex.align-items-center.justify-content-between a {
            margin-bottom: 0.5rem;
        }

    }

    /* Extra small devices (< 400px) */
    @@media (max-width: 399.98px) {
        .py-5

    {
        padding-top: 1.5rem !important;
        padding-bottom: 1.5rem !important;
    }

    .input-group .input-group-text {
        padding: 0.375rem 0.5rem;
    }

    .btn {
        padding-left: 0.75rem !important;
        padding-right: 0.75rem !important;
    }

    .form-control, .form-select {
        font-size: 0.9rem;
    }

    }
</style>